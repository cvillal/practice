const prompt = require('prompt-sync')();




5. if ussSchwarzenegger destroys (ship1) then attack (ship2) or (retreat)aka gameover(exit)
//else^^?
4. if (ship1) survives it attacks me again
3. if ussSchwarzenegger survives the it attacks (ship1)
2. if ship survives it attacks ussSchwarzenegger
//into a loop that does ^^
1. battle (ship1) //function

// const battle = (me, alien) => {
//     let number of ships = [1-6]?

//         while(schwarzShip.hull>0 && alienShip.hull>0) {
            
            
//             if ship survives (hull>0){
//                 ship.firepower? attack (ussSchwarzenegger)
//         }



const schwarzShip = {
    hull:20,
    firepower:5,
    accuracy:.7
    schwarzShipAttack = (enemy) => {
    enemy.hull -= schwarzShip.firepower



    // console.log ()
    if (schwarzShip.hull>0 && alienShip.hull>0){
        attackagain
    }



// if (Math.random()<ship[0].accuracy){
//     console.log('You have been hit!');
// }
// ^^do we even need this if we are just gunna run the same ship 6 times??^^




while (alienShip >0) {}
    continue to run battle ()
 

}




const alienShip = {
    hull: Math.round(3 + Math.random()*3),
    firepower: Math.round(2 + Math.random()*2),
    accuracy: (.6 + Math.random()*.2)

}



const battleEncounter= (enemy) => {
    let alienShipApproach = prompt("attack or retrieve")
        if (alienShipApproach === ("Attack")){
        //battleFunction()
            if(Math.random(<schwarzShip.accuracy))
                console.log ("You successfuly hit" $())
        }else {quit}
}